# WordPress core / vendor — this repo holds only the theme, plugin and docs
wp-content/
wp-admin/
wp-includes/
wp-config.php
vendor/
node_modules/

# Local env
*.log
.DS_Store
Thumbs.db
~$*

# Scraped Squarespace content — regenerate with scripts/scrape-squarespace.js
# (kept out of the public repo; it is ILANEL's copy, not POC code)
data/scraped-*.wxr.xml
